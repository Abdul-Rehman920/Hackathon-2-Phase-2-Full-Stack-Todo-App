# Skill: Recommended Monorepo Structure

This is the current and target structure for our Hackathon Phase 2 Todo full-stack project.

fullstack-todo/
├── .claude/                   # Claude Projects workspace (already exists)
│   ├── agents/                # 6 agents (spec-writer, architecture-planner, etc.)
│   ├── commands/              # Future custom commands
│   └── skills/                # Reusable knowledge (this folder - already exists)
├── .spec-kit/                 # Spec-Kit Plus config (already exists)
│   └── config.yaml            # Will define phases and spec folders
├── specs/                     # ← TO BE GENERATED by spec-writer agent
│   ├── overview.md            # Project overview
│   ├── features/              # Feature specs
│   │   ├── task-crud.md
│   │   └── authentication.md
│   ├── api/                   # API specifications
│   │   └── rest-endpoints.md
│   ├── database/              # DB schema
│   │   └── schema.md
│   └── ui/                    # UI component and page descriptions
│       ├── pages.md
│       └── components.md
├── frontend/                  # ← TO BE CREATED: Next.js app
├── backend/                   # ← TO BE CREATED: FastAPI app
├── CLAUDE.md                  # Root instructions (already exists)
├── README.md                  # Project setup guide (create soon)
├── docker-compose.yml         # For local dev (to be added)
├── .env.example               # Environment variables template
└── (package.json, pyproject.toml, etc. will be added)